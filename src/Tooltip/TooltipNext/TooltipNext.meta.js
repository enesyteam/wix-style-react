import TooltipNext from './TooltipNext';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TooltipNext);

metadata.exportedFrom({
  path: 'src/Tooltip/TooltipNext/TooltipNext.js',
});
